name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Compile Python sources
        run: python -m compileall -q src alembic

      - name: Validate Alembic migrations
        run: alembic upgrade head --sql > /tmp/gqmrmed-schema.sql

      - name: Ruff
        run: ruff check .

      - name: MyPy
        run: mypy src

      - name: Verify source package resolution
        run: |
          python -c "from pathlib import Path; import gqmrmed.contracts.generation as m; source = Path('src/gqmrmed').resolve(); loaded = Path(m.__file__).resolve(); assert loaded.is_relative_to(source), f'Unexpected package path: {loaded}'"

      - name: Pytest
        run: python -m pytest

      - name: Build production container
        run: docker build --tag gqmrmed:ci .

      - name: Smoke-test production container imports
        run: |
          docker run --rm --entrypoint python gqmrmed:ci -c "import gqmrmed.main; import gqmrmed.services.runtime"
