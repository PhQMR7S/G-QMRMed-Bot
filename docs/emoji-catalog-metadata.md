# Premium Emoji catalog diagnostics

The owner-only `/emoji_catalog` command displays each registered Premium Emoji with its Telegram custom emoji ID, Telegram-provided `alt`, sticker set name, and current semantic slot. This keeps the registry auditable without guessing mappings from opaque IDs.
